# Required for LLM analysis
OPENAI_API_KEY=

# Optional; defaults to gpt-4.1-mini
OPENAI_MODEL=gpt-4.1-mini

# Only needed for local development if frontend/backend are split.
# Vercel same-origin deployment does not require this.
CORS_ORIGINS=http://localhost:3000
